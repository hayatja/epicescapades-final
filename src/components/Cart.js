import '../styles/Cart.css';

const Navbar = () => {
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      asd
    </div>
  );
};

export default Navbar;
